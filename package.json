{
  "name": "image-tracing-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Client-side playground to evaluate deterministic raster-to-vector tracing algorithms side by side.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "imagetracerjs": "^1.2.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
